*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/
kotlin-js-store/

# Kotzilla SDK config — contains an API key, keep out of VCS
kotzilla.json

# Release upload keystore, decoded from a secret during CI — never commit it
androidApp/keystore/

# Code-review reports — throwaway artifacts written by the code-review skill
.claude/reviews/

# Local SwiftPM working area (multi-GB) of the Kotlin/Native SPM integration. That integration is
# off via kotlin.disableSwiftPMImport, so this only guards against it reappearing.
.swiftpm-locks/
