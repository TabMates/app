<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- POST_NOTIFICATIONS is deliberately not declared here. firebase-messaging and
         kmpnotifier-core each declare it in their own AAR manifest, so it merges into the Play
         flavor along with the Firebase dependency (as WAKE_LOCK and c2dm.RECEIVE already do) and
         is absent from the FOSS flavor, which has no notifications to ask for. Declaring it here
         would put it in the FOSS manifest too. -->

    <application
        android:name=".TabMatesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- Firebase meta-data lives in src/play/AndroidManifest.xml: its values are app-specific,
             so unlike the permissions above no AAR can supply them, and they must not appear in
             the FOSS manifest. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <!-- Must match the prod BASE_URL_PUBLIC host (and the web CNAME). -->
                <data android:host="app.tabmates.de" />

                <data android:pathPrefix="/verify/" />
                <data android:pathPrefix="/reset-password/" />
                <data android:pathPrefix="/join/" />
                <!-- Stateless "open the app" target. Carries no data: it exists so the web
                     client's intent:// promo has a path this app claims, and so a plain
                     https://app.tabmates.de/open link opens the app when it is installed.
                     Not "/" — claiming the bare host would swallow every web-client link. -->
                <data android:pathPrefix="/open" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="tabmates" />
                <data android:host="app.tabmates.de" />

                <data android:pathPrefix="/verify/" />
                <data android:pathPrefix="/reset-password/" />
                <data android:pathPrefix="/join/" />
                <data android:pathPrefix="/open" />
            </intent-filter>
        </activity>
    </application>

</manifest>